// Register the ScrollTrigger plugin
gsap.registerPlugin(ScrollTrigger);

// Select all divs with the class "fade-in-div"
const fadeInDivs = document.querySelectorAll(".fade-in1");



// Loop through each div and create a fade-in animation
fadeInDivs.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger1', // Trigger animation when this div enters the viewport
                start: "top 85%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});

// Select all divs with the class "fade-in-div"
const fadeInDivs2 = document.querySelectorAll(".fade-in2");

// Loop through each div and create a fade-in animation
fadeInDivs2.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger2', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});



// Select all divs with the class "fade-in-div"
const fadeInDivs3 = document.querySelectorAll(".fade-in3");

// Loop through each div and create a fade-in animation
fadeInDivs3.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger3', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});




// Select all divs with the class "fade-in-div"
const fadeInDivs4 = document.querySelectorAll(".fade-in4");

// Loop through each div and create a fade-in animation
fadeInDivs4.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger4', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});

// Select all divs with the class "fade-in-div"
const fadeInDivs5 = document.querySelectorAll(".fade-in5");

// Loop through each div and create a fade-in animation
fadeInDivs5.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger5', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});


// Select all divs with the class "fade-in-div"
const fadeInDivs6 = document.querySelectorAll(".fade-in6");

// Loop through each div and create a fade-in animation
fadeInDivs6.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger6', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});

const fadeInDivs7 = document.querySelectorAll(".fade-in7");

// Loop through each div and create a fade-in animation
fadeInDivs7.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger7', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});

const fadeInDivs8 = document.querySelectorAll(".fade-in8");

// Loop through each div and create a fade-in animation
fadeInDivs8.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger8', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});

const fadeInDivs9 = document.querySelectorAll(".fade-in9");

// Loop through each div and create a fade-in animation
fadeInDivs9.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger9', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});

const fadeInDivs10 = document.querySelectorAll(".fade-in10");

// Loop through each div and create a fade-in animation
fadeInDivs10.forEach((div) => {
    gsap.fromTo(
        div,
        { opacity: 0, y: 50 }, // Start state: invisible and slightly below
        {
            opacity: 1,
            ease: "power2.out",
            y: 0, // End state: fully visible and in place
            duration: 1, // Animation duration
            scrollTrigger: {
                trigger: '.trigger10', // Trigger animation when this div enters the viewport
                start: "top 65%", // Start when the top of the div is 80% down the viewport
                end: "top -25%", // End when the top of the div is 20% down the viewport
                toggleActions: "play reverse play reverse", // Play forward on scroll down, reverse on scroll up
                markers: false, // Set to true if you want to see debug markers
            },
        }
    );
});